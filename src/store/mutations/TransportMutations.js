import {TransportMutationTypes} from "./types";

const TransportMutations = {};

mutations[TransportMutationTypes.ADD_TRANSPORT] = function (state, {description}) {
    state.trans = isLogged;
    state.isRegisterUser = isRegisterUser;
};

export default TransportMutations;
