import {TransportActionTypes} from "./types";

const actions = {};

actions[TransportActionTypes.ADD_TRANSPORT] = function ({commit, state, dispatch}, tokenValue) {

};

actions[TransportActionTypes.DELETE_TRANSPORT] = function ({commit, state, dispatch}, tokenValue) {

};

actions[TransportActionTypes.CHANGE_CURRENT_TRANSPORT] = function ({commit, state, dispatch}, tokenValue) {

};

export default actions;
